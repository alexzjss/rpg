# Aba de Itens — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Catálogo global de itens (modelos) referenciado por personagens, com aba própria de cartas de item, inventário na aba Personagens, e receitas/loja reconectadas ao catálogo.

**Architecture:** Novo array global `items: Item[]` (modelos) espelhando `cards`. Personagens passam a referenciar via `ownedItems: {itemId, quantity}[]`. Um resolvedor (`utils/items.ts`) reconstrói itens completos para combate/inventário/receitas. Helpers puros para dar/consumir/ajustar quantidade.

**Tech Stack:** React 19, TypeScript, Vite, IndexedDB.

> **Restrições do projeto:** sem git (cada "checkpoint" = `npm run build` verde) e sem test runner (verificação = `npm run build` + `npx tsc --noEmit` sem **novos** erros; baseline = **29** erros pré-existentes). Spec: `docs/superpowers/specs/2026-06-17-items-tab-design.md`.

---

## Estrutura de arquivos

- **Criar** `utils/items.ts` — `ResolvedItem`, `resolveOwnedItems`, `giveOwned`, `consumeOwned`, `setOwnedQuantity`, `removeOwned` (puros).
- **Modificar** `types.ts` — `OwnedItem`, `Character.ownedItems`.
- **Modificar** `utils/database.ts` — store `'items'`, version bump, métodos `syncItems/saveItem/deleteItem`, snapshot, `ensureChar`.
- **Modificar** `App.tsx` — estado `items`, `ensureTemplate`, aba Itens, cartas de item, modal "dar a personagem", repontar inventário/combate/receitas/loja.

---

## Task 1: Tipos de posse referenciada

**Files:**
- Modify: `types.ts` (após `interface Item`, ~linha 77; e dentro de `interface Character`, ~linha 149)

- [ ] **Step 1: Adicionar `OwnedItem` e o campo `ownedItems`**

Em `types.ts`, logo após o fechamento de `export interface Item { ... }` (linha ~77), adicionar:

```ts
/** Posse referenciada de um item do catálogo global. */
export interface OwnedItem {
  itemId: string;
  quantity: number;
}
```

Dentro de `export interface Character { ... }`, logo após a linha `items: Item[];` (~149), adicionar:

```ts
  ownedItems?: OwnedItem[]; // posse referenciada (catálogo global)
```

- [ ] **Step 2: Checkpoint**

Run: `npm run build`
Expected: build OK (campo opcional, ninguém usa ainda).

---

## Task 2: Resolvedor e helpers puros

**Files:**
- Create: `utils/items.ts`

- [ ] **Step 1: Escrever o módulo**

Criar `utils/items.ts`:

```ts
import { Character, Item, OwnedItem } from '../types';

/** Item do catálogo com a quantidade possuída anexada. */
export interface ResolvedItem extends Item {
  quantity: number;
}

/** Reconstrói os itens completos que um personagem possui, a partir do catálogo.
 *  Referências órfãs (modelo deletado) são ignoradas. */
export function resolveOwnedItems(char: Character, catalog: Item[]): ResolvedItem[] {
  const owned = char.ownedItems ?? [];
  const out: ResolvedItem[] = [];
  for (const o of owned) {
    const tpl = catalog.find(i => i.id === o.itemId);
    if (!tpl) continue;
    out.push({ ...tpl, quantity: o.quantity });
  }
  return out;
}

/** Retorna um novo array ownedItems com +qty do item (cria a entrada se faltar). */
export function giveOwned(char: Character, itemId: string, qty: number): OwnedItem[] {
  const owned = [...(char.ownedItems ?? [])];
  const idx = owned.findIndex(o => o.itemId === itemId);
  if (idx >= 0) owned[idx] = { ...owned[idx], quantity: owned[idx].quantity + qty };
  else owned.push({ itemId, quantity: qty });
  return owned;
}

/** Retorna um novo array ownedItems com -qty (remove a entrada se chegar a 0). */
export function consumeOwned(char: Character, itemId: string, qty: number): OwnedItem[] {
  const owned = [...(char.ownedItems ?? [])];
  const idx = owned.findIndex(o => o.itemId === itemId);
  if (idx < 0) return owned;
  const newQty = owned[idx].quantity - qty;
  if (newQty <= 0) owned.splice(idx, 1);
  else owned[idx] = { ...owned[idx], quantity: newQty };
  return owned;
}

/** Define a quantidade exata (remove se <= 0). */
export function setOwnedQuantity(char: Character, itemId: string, qty: number): OwnedItem[] {
  if (qty <= 0) return (char.ownedItems ?? []).filter(o => o.itemId !== itemId);
  const owned = [...(char.ownedItems ?? [])];
  const idx = owned.findIndex(o => o.itemId === itemId);
  if (idx >= 0) owned[idx] = { ...owned[idx], quantity: qty };
  else owned.push({ itemId, quantity: qty });
  return owned;
}

/** Remove totalmente a posse de um item. */
export function removeOwned(char: Character, itemId: string): OwnedItem[] {
  return (char.ownedItems ?? []).filter(o => o.itemId !== itemId);
}
```

- [ ] **Step 2: Checkpoint**

Run: `npm run build`
Expected: build OK.

---

## Task 3: Persistência do catálogo

**Files:**
- Modify: `utils/database.ts` (ALL_STORES ~92, IDB_VERSION ~90, AppSnapshot ~43, ensureChar ~179, loadAll ~287, métodos ~329, initialize ~312, saveFullSnapshot ~420, restoreSnapshot ~455)

- [ ] **Step 1: Adicionar o store e bump da versão**

Localizar (linhas ~88-92):

```ts
const IDB_NAME = 'rpg_master_db';
// bump para 4 → garante onupgradeneeded nos browsers que ainda têm v1/v2/v3
const IDB_VERSION = 4;

const ALL_STORES = ['characters', 'cards', 'seals', 'meta'] as const;
```

Substituir por:

```ts
const IDB_NAME = 'rpg_master_db';
// bump para 5 → cria o store 'items' (catálogo global)
const IDB_VERSION = 5;

const ALL_STORES = ['characters', 'cards', 'seals', 'items', 'meta'] as const;
```

- [ ] **Step 2: Adicionar `items` ao `AppSnapshot`**

Localizar (linha ~48) e adicionar a linha `items`:

```ts
  characters: Character[];
  cards: Card[];
  items: Item[];
  seals: Seal[];
```

> `Item` já é importado no topo de `database.ts` (`import { Card, Character, CombatState, JourneyState, Seal } from '../types';`). **Adicionar `Item`** a esse import: `import { Card, Character, CombatState, Item, JourneyState, Seal } from '../types';`

- [ ] **Step 3: `ensureChar` garante `ownedItems`**

Localizar (linha ~179-181):

```ts
function ensureChar(c: any): Character {
  return { ...c, items: c.items ?? [], conditions: c.conditions ?? [], cardIds: c.cardIds ?? [] };
}
```

Substituir por:

```ts
function ensureChar(c: any): Character {
  return { ...c, items: c.items ?? [], ownedItems: c.ownedItems ?? [], conditions: c.conditions ?? [], cardIds: c.cardIds ?? [] };
}
```

- [ ] **Step 4: `loadAll` carrega o catálogo**

Localizar (linhas ~288-303) e adicionar `items` ao Promise.all e ao retorno:

```ts
async function loadAll() {
  const [chars, cards, items, seals, combatRec, journeyRec, extrasRec] = await Promise.all([
    _getAll<any>('characters'),
    _getAll<any>('cards'),
    _getAll<any>('items'),
    _getAll<any>('seals'),
    _get<any>('meta', '__combat'),
    _get<any>('meta', '__journey'),
    _get<any>('meta', '__extras'),
  ]);
  return {
    characters: chars.map(ensureChar),
    cards: cards as Card[],
    items: items as Item[],
    seals: seals.map(ensureSeal) as Seal[],
    combat: ensureCombat(combatRec?.value),
    journey: ensureJourney(journeyRec?.value),
    extras: ensureExtras(extrasRec?.value),
  };
}
```

- [ ] **Step 5: `initialize` declara `items` no tipo de retorno**

Localizar (linhas ~312-319) e adicionar `items: Item[];`:

```ts
  initialize: async (): Promise<{
    characters: Character[];
    cards: Card[];
    items: Item[];
    seals: Seal[];
    combat: CombatState;
    journey: JourneyState;
    extras: AppExtras;
  }> => {
    await runMigrations();
    return loadAll();
  },
```

- [ ] **Step 6: Métodos `syncItems/saveItem/deleteItem`**

Logo após o bloco `syncCards` (~linha 332), adicionar:

```ts
  syncItems: (cb: (d: Item[]) => void) => {
    _getAll<Item>('items').then(d => cb(d)).catch(() => cb([]));
    return _subscribe<Item[]>('items', cb);
  },
```

E logo após o bloco `deleteCard` (procurar `deleteCard: async (id: string) => { ... }`), adicionar:

```ts
  saveItem: async (item: Item) => {
    await _put('items', item);
    _notify('items', await _getAll<Item>('items'));
  },
  deleteItem: async (id: string) => {
    await _delete('items', id);
    _notify('items', await _getAll<Item>('items'));
  },
```

- [ ] **Step 7: Registrar a chave `items` nos listeners**

Localizar (linha ~218-221):

```ts
type ListenerKey = 'characters' | 'cards' | 'seals' | 'combat' | 'journey' | 'extras';
const _listeners: Record<ListenerKey, Function[]> = {
  characters: [], cards: [], seals: [], combat: [], journey: [], extras: [],
};
```

Substituir por:

```ts
type ListenerKey = 'characters' | 'cards' | 'items' | 'seals' | 'combat' | 'journey' | 'extras';
const _listeners: Record<ListenerKey, Function[]> = {
  characters: [], cards: [], items: [], seals: [], combat: [], journey: [], extras: [],
};
```

- [ ] **Step 8: `saveFullSnapshot` e `restoreSnapshot` incluem `items`**

Em `saveFullSnapshot` (~421-437), adicionar ao `Promise.all` após `_replaceAll('cards', ...)`:

```ts
      _replaceAll('items', snapshot.items),
```

E após `_notify('cards', snapshot.cards);` adicionar:

```ts
    _notify('items', snapshot.items);
```

Em `restoreSnapshot` (~455-476), adicionar após `cards: ...`:

```ts
        items: Array.isArray(raw.items) ? raw.items : [],
```

- [ ] **Step 9: Checkpoint**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; contagem de erros tsc = **29** (sem novos).

---

## Task 4: Estado do catálogo + `ensureTemplate` no App

**Files:**
- Modify: `App.tsx` (estado ~3539; boot subscriptions ~3765; helpers perto de outros use* )

- [ ] **Step 1: Importar helpers e tipos**

Localizar o import de tema adicionado antes (`import { DAMAGE_TYPES, PIN_COLORS, CARD_TYPE_THEME, type CardTypeStyle } from './utils/theme';`) e adicionar abaixo:

```tsx
import { resolveOwnedItems, giveOwned, consumeOwned, setOwnedQuantity, removeOwned, type ResolvedItem } from './utils/items';
```

Garantir que `OwnedItem` esteja no import de `./types` (adicionar `OwnedItem` à lista de imports de `'./types'` no topo).

- [ ] **Step 2: Estado `items`**

Localizar (`const [cards, setCards] = useState<Card[]>([]);` — procurar) e adicionar abaixo:

```tsx
  const [items, setItems] = useState<Item[]>([]);
```

- [ ] **Step 3: Carregar no boot**

No `useEffect` de boot, no destructuring de `DatabaseService.initialize().then(({ characters: chars, cards: cds, seals: sls, combat: cbt, journey: jny, extras }) => {`, adicionar `items: its,`:

```tsx
    DatabaseService.initialize().then(({ characters: chars, cards: cds, items: its, seals: sls, combat: cbt, journey: jny, extras }) => {
```

E logo após `setCards(cds);` adicionar:

```tsx
      setItems(its);
```

Nas subscriptions (após `const unsubCards = ...`), adicionar:

```tsx
    const unsubItems = DatabaseService.syncItems((data) => { if (!cancelled) setItems(data); });
```

E no cleanup (`unsubChars(); unsubCards(); ...`), adicionar `unsubItems();`.

- [ ] **Step 4: Incluir `items` no autosave/snapshot**

Localizar as chamadas `DatabaseService.saveFullSnapshot({` (há 2 — autosave ~3786 e outra ~4013) e adicionar `items,` ao objeto passado (logo após `cards,`). Exemplo do objeto:

```tsx
        await DatabaseService.saveFullSnapshot({
          version: SNAPSHOT_VERSION,
          savedAt: new Date().toISOString(),
          characters,
          cards,
          items,
          seals,
          combat: combat!,
          journey: journey!,
          extras: { ...extrasObj },
        });
```

> Conferir os nomes reais das variáveis em cada call site; só inserir a linha `items,`.

- [ ] **Step 5: `ensureTemplate` helper**

Perto de `handleSaveItem` (~4126), adicionar:

```tsx
  /** Garante que existe um modelo no catálogo com aquele nome; cria se faltar. Retorna o itemId. */
  const ensureTemplate = (name: string, seed: Partial<Item>): string => {
    const existing = items.find(i => i.name.toLowerCase() === name.toLowerCase());
    if (existing) return existing.id;
    const newItem: Item = {
      id: Math.random().toString(36).substr(2, 9),
      name,
      description: seed.description ?? '',
      image: seed.image ?? '',
      ...seed,
    } as Item;
    DatabaseService.saveItem(newItem);
    return newItem.id;
  };
```

- [ ] **Step 6: Checkpoint**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; tsc = 29.

---

## Task 5: Aba "Itens" (catálogo de cartas)

**Files:**
- Modify: `App.tsx` (activeTab union ~3546; TabButtons ~6236; nova seção de render perto da aba `cards` ~9531)

- [ ] **Step 1: Adicionar `'items'` ao tipo de aba**

Localizar `useState<'combat' | 'cards' | 'seals' | 'characters' | 'extras' | 'journey'>('combat')` e adicionar `| 'items'`:

```tsx
  const [activeTab, setActiveTab] = useState<'combat' | 'cards' | 'items' | 'seals' | 'characters' | 'extras' | 'journey'>('combat');
```

- [ ] **Step 2: Botão da aba**

Localizar o TabButton de Habilidades (`active={activeTab === 'cards'} onClick={() => setActiveTab('cards')}>Habilidades</TabButton>`) e adicionar logo depois:

```tsx
              <TabButton icon={<Backpack className="w-4 h-4" />} active={activeTab === 'items'} onClick={() => setActiveTab('items')}>Itens</TabButton>
```

> Importar `Backpack` de `lucide-react` (adicionar ao import do topo). Se `Backpack` não existir na versão, usar `Package`.

- [ ] **Step 3: Render da aba**

Localizar o fim da aba de Habilidades (`{activeTab === 'cards' && ( ... )}` — encontrar seu fechamento `)}`) e adicionar logo depois a nova seção:

```tsx
        {/* Aba Itens (catálogo) */}
        {activeTab === 'items' && (
          <div className="space-y-8 anim-fade-up" style={{ height:'100%', overflowY:'auto' }}>
            <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
              <div>
                <h2 className="text-4xl font-black text-white uppercase italic tracking-tight">Arsenal</h2>
                <p className="text-slate-600 font-bold uppercase tracking-[0.3em] text-xs mt-1">Catálogo de Itens</p>
              </div>
              <div className="flex gap-3 w-full md:w-auto">
                <div className="relative flex-1 md:flex-none">
                  <Search className="w-4 h-4 text-slate-500 absolute left-4 top-1/2 -translate-y-1/2" />
                  <input type="text" placeholder="Buscar..." className="w-full md:w-64 bg-slate-900/80 border border-slate-800 rounded-xl pl-10 pr-5 py-3 text-sm text-white focus:border-amber-600 outline-none" value={itemSearchTerm} onChange={e => setItemSearchTerm(e.target.value)} />
                </div>
                <button onClick={() => { setEditingCatalogItem({} as Item); }} className="flex items-center gap-2 px-5 py-3 rounded-xl bg-amber-700 hover:bg-amber-600 text-white text-sm font-bold uppercase tracking-wider whitespace-nowrap">
                  <Plus className="w-4 h-4" /> Novo Item
                </button>
              </div>
            </div>

            <div style={{ display:'grid', gridTemplateColumns:'repeat(auto-fill,minmax(210px,1fr))', gap:14 }}>
              {items.filter(it => it.name.toLowerCase().includes(itemSearchTerm.toLowerCase())).map(it => (
                <div key={it.id} onClick={() => setEditingCatalogItem(it)} style={{ cursor:'pointer', borderRadius:16, overflow:'hidden', border:'1px solid var(--border-gold)', background:'linear-gradient(165deg, rgba(40,30,5,0.85), rgba(20,16,8,0.92))', position:'relative' }} className="hover:brightness-110 transition-all">
                  <div style={{ height:120, background: it.image ? `url(${it.image}) center/cover` : 'linear-gradient(145deg,#1e180e,#100e08)' }} />
                  <div style={{ padding:'10px 12px' }}>
                    <p style={{ fontSize:13, fontWeight:800, color:'var(--gold-pale)', overflow:'hidden', textOverflow:'ellipsis', whiteSpace:'nowrap' }}>{it.name}</p>
                    {it.category && <p style={{ fontSize:9, color:'var(--text-muted)', textTransform:'uppercase', letterSpacing:'0.1em', marginTop:2 }}>{it.category}</p>}
                    {it.usableInCombat && (
                      <div style={{ display:'flex', gap:6, marginTop:6, flexWrap:'wrap' }}>
                        {(it.combatHeal ?? 0) > 0 && <span style={{ fontSize:9, color:'#34d399' }}>+{it.combatHeal}♥</span>}
                        {(it.combatDamage ?? 0) > 0 && <span style={{ fontSize:9, color:'#f87171' }}>{it.combatDamage}⚔</span>}
                        {(it.combatAuraRecover ?? 0) > 0 && <span style={{ fontSize:9, color:'#a78bfa' }}>+{it.combatAuraRecover}⚡</span>}
                      </div>
                    )}
                  </div>
                  <button onClick={e => { e.stopPropagation(); setGiveItemTarget(it); }} title="Dar a personagem" style={{ position:'absolute', top:8, right:8, width:28, height:28, borderRadius:8, background:'rgba(22,27,38,0.92)', border:'1px solid var(--border-gold)', color:'var(--gold-mid)', display:'flex', alignItems:'center', justifyContent:'center', cursor:'pointer' }}>
                    <UserPlus style={{ width:13, height:13 }} />
                  </button>
                </div>
              ))}
              {items.length === 0 && (
                <p style={{ gridColumn:'1/-1', textAlign:'center', color:'var(--text-muted)', padding:'40px 0' }}>Nenhum item no catálogo. Clique em "Novo Item".</p>
              )}
            </div>
          </div>
        )}
```

- [ ] **Step 4: Estados de UI da aba**

Perto dos outros `useState` de UI (ex: `searchTerm`), adicionar:

```tsx
  const [itemSearchTerm, setItemSearchTerm] = useState('');
  const [editingCatalogItem, setEditingCatalogItem] = useState<Item | null>(null);
  const [giveItemTarget, setGiveItemTarget] = useState<Item | null>(null);
```

- [ ] **Step 5: Checkpoint**

Run: `npm run build`
Expected: build OK. (Os modais de `editingCatalogItem`/`giveItemTarget` ainda não renderizam — Task 6.)

---

## Task 6: Editar modelo (ItemForm) + modal "Dar a personagem"

**Files:**
- Modify: `App.tsx` (perto dos outros modais no fim do render, ~11342 onde o `ItemForm` já é usado)

- [ ] **Step 1: Modal de edição do catálogo (reusa ItemForm)**

Perto do uso existente do `ItemForm` (~11342), adicionar um novo bloco de modal para o catálogo:

```tsx
      {editingCatalogItem && (
        <Modal title={editingCatalogItem.id ? 'Editar Item' : 'Novo Item'} onClose={() => setEditingCatalogItem(null)}>
          <ItemForm
            initialData={editingCatalogItem.id ? editingCatalogItem : undefined}
            onSubmit={(item) => {
              const toSave: Item = item.id ? item : { ...item, id: Math.random().toString(36).substr(2, 9) };
              DatabaseService.saveItem(toSave);
              setEditingCatalogItem(null);
            }}
            onDelete={(id) => { DatabaseService.deleteItem(id); setEditingCatalogItem(null); }}
          />
        </Modal>
      )}
```

> Conferir a assinatura de `Modal` (`title`, `onClose`, `children`) — já existe em `App.tsx:355`.

- [ ] **Step 2: Modal "Dar a personagem"**

Adicionar logo abaixo:

```tsx
      {giveItemTarget && (
        <Modal title={`Dar "${giveItemTarget.name}"`} onClose={() => setGiveItemTarget(null)}>
          <div style={{ display:'flex', flexDirection:'column', gap:10, padding:4 }}>
            <div style={{ display:'flex', alignItems:'center', gap:10 }}>
              <span style={{ fontSize:11, color:'var(--text-muted)', textTransform:'uppercase', letterSpacing:'0.1em' }}>Quantidade</span>
              <input type="number" min={1} value={giveItemQty} onChange={e => setGiveItemQty(Math.max(1, parseInt(e.target.value) || 1))} style={{ width:80, background:'var(--bg-raised)', border:'1px solid var(--border-mid)', borderRadius:8, padding:'6px 10px', color:'var(--text-primary)' }} />
            </div>
            <div style={{ display:'grid', gridTemplateColumns:'repeat(auto-fill,minmax(150px,1fr))', gap:8, maxHeight:320, overflowY:'auto' }}>
              {characters.map(ch => (
                <button key={ch.id} onClick={() => {
                  updateCharacterStats(ch.id, { ownedItems: giveOwned(ch, giveItemTarget.id, giveItemQty) });
                  setGiveItemTarget(null);
                  setGiveItemQty(1);
                }} style={{ display:'flex', alignItems:'center', gap:8, padding:'8px 10px', background:'var(--bg-raised)', border:'1px solid var(--border-faint)', borderRadius:10, cursor:'pointer', textAlign:'left' }} className="hover:border-amber-700/50">
                  <div style={{ width:30, height:30, borderRadius:8, overflow:'hidden', flexShrink:0, background:'var(--bg-base)' }}>
                    {ch.icon && <img src={ch.icon} style={{ width:'100%', height:'100%', objectFit:'cover' }} />}
                  </div>
                  <span style={{ fontSize:12, fontWeight:700, color:'var(--text-primary)', overflow:'hidden', textOverflow:'ellipsis', whiteSpace:'nowrap' }}>{ch.name}</span>
                </button>
              ))}
            </div>
          </div>
        </Modal>
      )}
```

- [ ] **Step 3: Estado da quantidade**

Perto de `giveItemTarget`, adicionar:

```tsx
  const [giveItemQty, setGiveItemQty] = useState(1);
```

> `updateCharacterStats(id, partial)` já existe e aceita `Partial<Character>`; `{ ownedItems }` é válido. Conferir que `giveOwned` está importado (Task 4 Step 1).

- [ ] **Step 4: Checkpoint**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; tsc = 29.
Verificação visual (usuário): aba Itens → Novo Item → salvar → aparece carta → ícone "dar a personagem" → escolher personagem.

---

## Task 7: Inventário na aba Personagens (repoint)

**Files:**
- Modify: `App.tsx` (`handleSaveItem` ~4126, `handleDeleteItem` ~4140, e o modal de inventário aberto por `openInventoryCharId`)

- [ ] **Step 1: `handleSaveItem` salva no catálogo e dá ao personagem**

Localizar (`~4126-4136`):

```tsx
  const handleSaveItem = (item: Item) => {
    if (!selectedInventoryChar) return;
    let updatedItems;
    if (item.id && selectedInventoryChar.items.some(i => i.id === item.id)) {
        updatedItems = selectedInventoryChar.items.map(i => i.id === item.id ? item : i);
    } else {
        const finalItem = { ...item, id: item.id || Math.random().toString(36).substr(2, 9) };
        updatedItems = [...(selectedInventoryChar.items || []), finalItem];
    }
    updateCharacterStats(selectedInventoryChar.id, { items: updatedItems });
  };
```

> O trecho real pode diferir levemente; ler o bloco antes de editar. Substituir o corpo por uma versão referenciada:

```tsx
  const handleSaveItem = (item: Item) => {
    if (!selectedInventoryChar) return;
    const id = item.id || Math.random().toString(36).substr(2, 9);
    const toSave: Item = { ...item, id };
    DatabaseService.saveItem(toSave); // grava o modelo no catálogo
    // garante que o personagem possui ao menos 1 (se ainda não possui)
    const alreadyOwned = (selectedInventoryChar.ownedItems ?? []).some(o => o.itemId === id);
    if (!alreadyOwned) {
      updateCharacterStats(selectedInventoryChar.id, { ownedItems: giveOwned(selectedInventoryChar, id, item.quantity ?? 1) });
    }
    setEditingItem(null);
  };
```

- [ ] **Step 2: `handleDeleteItem` remove a posse (não o modelo)**

Localizar (`~4140-4146`):

```tsx
        const updatedItems = selectedInventoryChar.items.filter(i => i.id !== itemId);
        updateCharacterStats(selectedInventoryChar.id, { items: updatedItems });
```

Substituir por:

```tsx
        updateCharacterStats(selectedInventoryChar.id, { ownedItems: removeOwned(selectedInventoryChar, itemId) });
```

- [ ] **Step 3: Repontar o render do inventário**

Ler o modal aberto por `openInventoryCharId` (procurar `openInventoryCharId` no JSX). Onde ele lista `char.items` (ou `selectedInventoryChar.items`), trocar a fonte por:

```tsx
resolveOwnedItems(char, items)
```

E os controles de quantidade (+/−) devem chamar:

```tsx
updateCharacterStats(char.id, { ownedItems: setOwnedQuantity(char, it.id, novaQuantidade) })
```

onde `it` é o `ResolvedItem` (seu `it.id` é o itemId do modelo). O botão remover usa `removeOwned(char, it.id)`.

> Como o render exato varia, ler o bloco do inventário antes de editar e aplicar estas substituições mantendo o layout existente.

- [ ] **Step 4: Checkpoint**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; tsc = 29.

---

## Task 8: Combate (repoint de leitura/consumo)

**Files:**
- Modify: `App.tsx` (`CombatItemPanel` actor.items ~2329; `handleUseItem` decrementos ~4626/4643/4677; possíveis outros)

- [ ] **Step 1: `CombatItemPanel` recebe itens resolvidos**

O `CombatItemPanel` lê `actor.items` (linha ~2329). Como o componente recebe `actor` por prop, a forma mais limpa é passar os itens resolvidos por prop. Localizar onde `<CombatItemPanel actor={actor} ... />` é usado (~8407) e trocar para incluir os itens resolvidos. Editar a assinatura do componente:

Localizar (`App.tsx:2322`):

```tsx
const CombatItemPanel: React.FC<{
```
e a linha `const combatItems = (actor.items || []).filter((it: any) => it.usableInCombat && (it.quantity ?? 1) > 0);` (~2329).

Adicionar uma prop `resolvedItems` ao tipo do componente e usar:

```tsx
  const combatItems = (resolvedItems || []).filter((it: any) => it.usableInCombat && (it.quantity ?? 1) > 0);
```

No call site (~8407):

```tsx
<CombatItemPanel actor={actor} resolvedItems={resolveOwnedItems(characters.find(c => c.id === actor.id) ?? actor, items)} onUseItem={(item) => handleUseItem(actor, item)} />
```

> Ler a assinatura/props atuais do `CombatItemPanel` antes de editar e inserir `resolvedItems` no tipo.

- [ ] **Step 2: Repontar os decrementos em `handleUseItem`**

Há três blocos idênticos (linhas ~4626, ~4643, ~4677) com este par exato de linhas:

```tsx
            const updatedItems = char.items.map((it: Item) => it.id !== item.id ? it : { ...it, quantity: Math.max(0, (it.quantity ?? 1) - 1) });
            updateCharacterStats(actor.id, { items: updatedItems });
```

Substituir **cada ocorrência** por:

```tsx
            updateCharacterStats(actor.id, { ownedItems: consumeOwned(char, item.id, 1) });
```

> Atenção à indentação de cada bloco (4626 e 4677 usam 12 espaços; conferir e casar). `item.id` aqui é o id do modelo (o painel agora passa `ResolvedItem`, cujo `.id` é o itemId).

- [ ] **Step 3: Verificar `applyItemEffects` e demais leituras de `char.items` em combate**

Rodar `grep -nE "char\.items|actor\.items|\.items\.map" App.tsx` e, para cada ocorrência ligada a combate/uso de item, aplicar o mesmo padrão: leitura → `resolveOwnedItems(char, items)`, consumo → `consumeOwned(char, id, n)`. (Linha ~4498, ~5461 inspecionar.)

- [ ] **Step 4: Checkpoint**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; tsc = 29.

---

## Task 9: Receitas (repoint)

**Files:**
- Modify: `App.tsx` `executeRecipe` (~4225-4273)

- [ ] **Step 1: Reescrever `executeRecipe` para o modelo referenciado**

Substituir a função inteira (`~4225-4273`) por:

```tsx
  const executeRecipe = (recipe: Recipe, characterId: string) => {
    const char = characters.find(c => c.id === characterId);
    if (!char) { alert('Personagem não encontrado.'); return; }
    const resolved = resolveOwnedItems(char, items);

    // Confere ingredientes (por nome)
    for (const ing of recipe.ingredients) {
      const owned = resolved.find(it => it.name.toLowerCase() === ing.itemName.toLowerCase());
      const available = owned?.quantity ?? 0;
      if (available < ing.quantity) {
        alert(`${char.name} não tem ${ing.quantity}x "${ing.itemName}" (tem ${available}).`);
        return;
      }
    }

    // Consome ingredientes
    let owned = char.ownedItems ?? [];
    for (const ing of recipe.ingredients) {
      const match = resolved.find(it => it.name.toLowerCase() === ing.itemName.toLowerCase());
      if (match) owned = consumeOwned({ ...char, ownedItems: owned }, match.id, ing.quantity);
    }

    // Produz resultado (garante modelo no catálogo, dá ao personagem)
    const resultId = ensureTemplate(recipe.resultItemName, {
      description: recipe.resultDescription || recipe.description,
      image: recipe.resultImage || '',
      category: recipe.resultCategory || (recipe.type === 'cozinhar' ? 'Comida' : 'Forjado'),
    });
    owned = giveOwned({ ...char, ownedItems: owned }, resultId, recipe.resultQuantity);

    const updatedChar = { ...char, ownedItems: owned };
    saveCharacter(updatedChar);
    setCraftResult({ recipe, character: updatedChar });
  };
```

> `ensureTemplate` (Task 4 Step 5) deve estar definido **antes** de `executeRecipe` no arquivo, ou no mesmo escopo do componente (ambos são closures do App — ok). `saveCharacter` e `setCraftResult` já existem.

- [ ] **Step 2: Checkpoint**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; tsc = 29.

---

## Task 10: Loja de upgrades (repoint)

**Files:**
- Modify: `App.tsx` (onde upgrades dão itens 'Upgrade', ~4420-4440)

- [ ] **Step 1: Localizar e ler o bloco**

Ler o trecho ~4415-4445 (procurar `category === 'Upgrade'` e `items: [...updatedChar.items, newItem]`).

- [ ] **Step 2: Repontar para o catálogo**

Onde o código hoje cria/incrementa um item 'Upgrade' em `updatedChar.items`, substituir pela criação de modelo + posse:

```tsx
        const upId = ensureTemplate(itemName, { category: 'Upgrade', description: upgradeDescription ?? '' });
        updatedChar = { ...updatedChar, ownedItems: giveOwned(updatedChar, upId, 1) };
```

> Ajustar os nomes locais (`itemName`, `upgradeDescription`) aos reais do bloco. Remover a lógica antiga de `find/map/push` em `updatedChar.items` para esse item. Manter o resto do fluxo de compra (débito de moedas etc.) intacto.

- [ ] **Step 3: Checkpoint final**

Run: `npm run build` && `npx tsc --noEmit 2>&1 | grep -cE "error TS"`
Expected: build OK; tsc = 29.

Verificação visual (usuário) — roteiro completo:
1. Aba **Itens** → criar um item (ex.: "Poção", usável em combate, +10 HP) → aparece no catálogo.
2. Ícone "dar a personagem" → dar 3x a um personagem.
3. Aba **Personagens** → abrir inventário desse personagem → ver "Poção x3"; testar +/− e remover.
4. **Combate** → menu "Itens" do personagem → a Poção aparece; usar → cura aplica e quantidade cai.
5. **Receita** (cozinhar/forjar) com ingrediente que o personagem tem → produzir → o resultado vira modelo no catálogo e aparece no inventário.
6. **Loja de upgrades** → comprar upgrade que dá item → item referenciado aparece.

---

## Notas
- Sem git: checkpoints = `npm run build` verde.
- Baseline tsc = 29 erros pré-existentes; nenhum novo deve ser introduzido.
- `Character.items` antigo permanece no tipo (não removido) para não quebrar imports, mas deixa de ser usado; inventários começam do zero via `ensureChar.ownedItems`.
- Funções puras em `utils/items.ts` ficam isoladas para teste futuro (sem runner agora).
